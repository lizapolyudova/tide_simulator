
// parameters section

// setup controllers
//

// setup the sun - center; 0,0
// setup the earth
// setup the moon

// groups:
// geo_group = earth + geo-static objects (observer cam; ideally, parameterizable); rotates around its own slightly tilted axis
// earth_moon_group = moon + geo_group; rotates around the sun + rotates around its own axis

